<% ['widget', 'video', 'videos', 'add_new_video', 'share'].each do |templ| %>
    <script type="text/x-handlebars" data-template-name="<%= templ %>">
      <%= render templ %>
    </script>
<% end %>
