<% ['widget', 'video', 'videos', 'add_new_video', 'share', 'edit', 'delete', 'error', 'landing', 'request_processing'].each do |templ| %>
    <script type="text/x-handlebars" data-template-name="<%= templ %>">
      <%= render templ %>
    </script>
<% end %>
